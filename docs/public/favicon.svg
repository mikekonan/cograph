<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32" role="img" aria-label="Cograph">
  <!-- The application's brand mark, verbatim from web/public/favicon.svg: a
       dashed orbital with three accent nodes. Keep the two in sync — a docs site
       with a different mark reads as a different product.

       Colours are literal because a favicon is fetched outside the page and
       cannot resolve CSS custom properties; 7c3aed is the accent token's value.
       Do not write a CSS custom property name in this comment: the two leading
       hyphens are illegal inside an XML comment, which silently made this file
       unparseable and the tab icon blank. -->
  <title>Cograph</title>
  <circle cx="16" cy="16" r="13" fill="none" stroke="#7c3aed" stroke-width="2.2"
          stroke-dasharray="20.23 7" stroke-dashoffset="-3.5" stroke-linecap="round" />
  <circle cx="16" cy="3" r="3.2" fill="#7c3aed" />
  <circle cx="4.74" cy="22.5" r="3.2" fill="#7c3aed" />
  <circle cx="27.26" cy="22.5" r="3.2" fill="#7c3aed" />
</svg>
