name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ./backend[dev]

      # tree-sitter-language-pack ships no grammars: get_parser() downloads them
      # from a GitHub release on first use, with no timeout. That put a network
      # round trip inside the test run, and on 2026-08-21 GitHub answered the
      # manifest request with a 504 — two integration tests failed with
      # `LanguageNotFoundError: Download error` and the whole job went red for a
      # reason no author could act on.
      #
      # Cache the grammars and fetch them in a step of their own. The key carries
      # the pinned pack version, which is what the cache path is namespaced by, so
      # a version bump misses the cache instead of serving grammars for the wrong
      # ABI. On a miss the fetch retries, and a genuine outage now fails a step
      # that says "grammars" rather than two tests that look like parser bugs.
      - name: Resolve the pinned tree-sitter-language-pack version
        id: grammars
        run: |
          version=$(sed -n 's/.*"tree-sitter-language-pack==\([^"]*\)".*/\1/p' backend/pyproject.toml)
          [ -n "$version" ] || { echo "::error::tree-sitter-language-pack is no longer pinned to an exact version"; exit 1; }
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v4
        with:
          path: ~/.cache/tree-sitter-language-pack
          key: tree-sitter-grammars-${{ runner.os }}-${{ steps.grammars.outputs.version }}

      - name: Fetch missing tree-sitter grammars
        run: |
          for attempt in 1 2 3; do
            if timeout 300 python -c "from backend.app.graph.parser import download_missing_grammars; download_missing_grammars()"; then
              break
            fi
            echo "::warning::grammar download attempt $attempt failed"
            [ "$attempt" = 3 ] && exit 1
            sleep $((attempt * 10))
          done
          python -c "from backend.app.graph.parser import missing_grammars; left = missing_grammars(); assert not left, f'grammars still missing: {left}'"

      - name: Create CI databases
        run: |
          python - <<'PY'
          import asyncio
          import asyncpg

          async def main() -> None:
              conn = await asyncpg.connect("postgresql://postgres:postgres@127.0.0.1:5432/postgres")
              try:
                  for db in ("cograph_ci", "cograph_test"):
                      await conn.execute(f'DROP DATABASE IF EXISTS "{db}"')
                      await conn.execute(f'CREATE DATABASE "{db}"')
              finally:
                  await conn.close()

          asyncio.run(main())
          PY

      - name: Ruff
        run: ruff check backend/app backend/tests backend/integration_tests

      - name: Pytest
        env:
          TEST_DATABASE_URL: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/cograph_test
        run: python -m pytest backend/tests -q

      - name: Alembic upgrade smoke
        env:
          COGRAPH_DATABASE__URL: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/cograph_ci
        run: alembic -c backend/alembic.ini upgrade head

      # The public REST and MCP references are generated from the app itself.
      # This runs here rather than in the Docs workflow because only this job has
      # the backend installed, and it needs no database — both the OpenAPI
      # document and the MCP tool list are built from the routers alone.
      - name: Generated references are current
        run: python scripts/gen_reference_docs.py --check

  wiki-schema-guard:
    name: Wiki schema guard
    # Diff-based, so it only makes sense on PRs where a base sha exists.
    # The precise companion (quality-surface sha test) runs in Pytest above.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check WIKI_SCHEMA_VERSION bump
        run: scripts/check_wiki_schema_version.sh "${{ github.event.pull_request.base.sha }}"

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "26"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build

  packaging:
    name: Packaging
    runs-on: ubuntu-latest
    timeout-minutes: 35
    needs:
      - backend
      - frontend
    steps:
      - uses: actions/checkout@v7

      - name: Validate docker-compose config
        run: docker compose config

      - name: Build container images
        run: docker compose build backend worker web

      - name: Smoke packaged stack
        env:
          COGRAPH_WEB_PORT: "18080"
        run: |
          docker compose up -d
          python - <<'PY'
          import http.client
          import time
          import urllib.error
          import urllib.request


          def fetch(url: str) -> tuple[int, str, str]:
              try:
                  with urllib.request.urlopen(url, timeout=5) as response:
                      return (
                          response.getcode(),
                          response.headers.get("Content-Type", ""),
                          response.read().decode("utf-8", errors="ignore"),
                      )
              except urllib.error.HTTPError as exc:
                  return (
                      exc.code,
                      exc.headers.get("Content-Type", ""),
                      exc.read().decode("utf-8", errors="ignore"),
                  )


          def wait_for_ok(url: str) -> None:
              deadline = time.time() + 120
              last_error: Exception | None = None
              while time.time() < deadline:
                  try:
                      status, _, _ = fetch(url)
                      if status == 200:
                          return
                  except Exception as exc:  # pragma: no cover - CI smoke only
                      last_error = exc
                  time.sleep(2)
              raise AssertionError(f"{url} did not become healthy") from last_error


          def fetch_raw(host: str, port: int, path: str) -> tuple[int, str, str, str]:
              connection = http.client.HTTPConnection(host, port, timeout=5)
              try:
                  connection.request("GET", path)
                  response = connection.getresponse()
                  body = response.read().decode("utf-8", errors="ignore")
                  return (
                      response.status,
                      response.getheader("Content-Type", ""),
                      response.getheader("Location", ""),
                      body,
                  )
              finally:
                  connection.close()


          wait_for_ok("http://127.0.0.1:18080/health")
          wait_for_ok("http://127.0.0.1:18080/api/health")

          status, content_type, location, body = fetch_raw("127.0.0.1", 18080, "/mcp")
          assert status not in {301, 302, 307, 308}, (
              f"/mcp unexpectedly redirected instead of normalizing internally: {status} -> {location}"
          )
          assert status != 404, f"/mcp unexpectedly returned 404: {body[:200]}"
          assert status != 421, f"/mcp rejected the proxied host header: {body[:200]}"
          assert status < 500, f"/mcp returned a server error: {status} {body[:200]}"
          assert "text/html" not in content_type.lower(), (
              "/mcp returned the SPA instead of the backend streamable HTTP app"
          )
          assert "<!doctype html>" not in body.lower(), (
              "/mcp returned index.html instead of the backend streamable HTTP app"
          )
          PY
          docker compose exec -T backend python - <<'PY'
          # Phase 30.2 made /mcp PAT-gated, so the smoke test mints a
          # throwaway user + PAT with `mcp` scope, then drives a real MCP
          # session through the packaged web tier with that bearer token.
          import asyncio
          import hashlib
          import secrets as stdlib_secrets

          import httpx
          from mcp import ClientSession
          from mcp.client.streamable_http import streamable_http_client

          from backend.app.config import get_settings
          from backend.app.core.deps import PAT_PLAINTEXT_PREFIX, PAT_PREFIX_DISPLAY_CHARS
          from backend.app.db.session import SessionManager
          from backend.app.models.enums import UserRole
          from backend.app.models.personal_access_token import PersonalAccessToken
          from backend.app.models.user import User


          async def _mint_pat(sm: SessionManager) -> str:
              secret = stdlib_secrets.token_urlsafe(36)
              plaintext = f"{PAT_PLAINTEXT_PREFIX}{secret}"
              token_hash = hashlib.sha256(plaintext.encode("utf-8")).digest()
              prefix = plaintext[:PAT_PREFIX_DISPLAY_CHARS]
              async with sm.session() as session:
                  user = User(
                      email=f"smoke-{stdlib_secrets.token_hex(4)}@example.com",
                      password_hash=None,
                      name="ci-smoke",
                      role=UserRole.OWNER,
                      auth_source="password",
                  )
                  session.add(user)
                  await session.flush()
                  pat = PersonalAccessToken(
                      user_id=user.id,
                      name="ci-smoke",
                      token_hash=token_hash,
                      token_prefix=prefix,
                      scopes=["mcp", "api:read"],
                  )
                  session.add(pat)
                  await session.commit()
              return plaintext


          async def main() -> None:
              settings = get_settings()
              sm = SessionManager(settings)
              try:
                  pat = await _mint_pat(sm)
              finally:
                  await sm.dispose()

              headers = {"Authorization": f"Bearer {pat}"}
              async with httpx.AsyncClient(
                  timeout=30, follow_redirects=True, headers=headers
              ) as http_client:
                  # Two values, not three: 2.0 dropped the get_session_id
                  # callable this smoke test never used from the tuple.
                  async with streamable_http_client(
                      "http://web/mcp", http_client=http_client
                  ) as (
                      read_stream,
                      write_stream,
                  ):
                      async with ClientSession(read_stream, write_stream) as session:
                          await session.initialize()
                          tools = await session.list_tools()

              tool_names = {tool.name for tool in tools.tools}
              expected = {
                  "cograph_repositories",
                  "cograph_collections",
                  "cograph_collection_document",
                  "cograph_collection_search",
                  "cograph_read_chunk",
                  "cograph_retrieve",
                  "cograph_read_node",
                  "cograph_search_code",
                  "cograph_related",
                  "cograph_repository_readme",
                  "cograph_read_file_range",
                  "cograph_outline",
                  "cograph_route",
                  "cograph_wiki_page",
              }
              assert tool_names == expected, f"unexpected MCP tools via packaged web tier: {tool_names}"


          asyncio.run(main())
          PY

      - name: Dump compose logs on smoke failure
        if: failure()
        run: docker compose logs --tail=200 backend worker web

      - name: Tear down packaged stack
        if: always()
        run: docker compose down -v

      - name: Setup Helm
        uses: azure/setup-helm@v5

      - name: Helm lint
        run: helm lint helm/cograph

      - name: Helm template
        run: helm template cograph helm/cograph > /tmp/cograph-helm.yaml

  tag:
    name: Auto tag
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - backend
      - frontend
      - packaging
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          # Pushing the tag with this PAT (contents: write) is what triggers
          # release.yml downstream — the default GITHUB_TOKEN deliberately
          # does not.
          token: ${{ secrets.RELEASE_PAT }}

      - name: Create and push tag
        id: tag
        run: |
          set -euo pipefail
          short_sha=$(git rev-parse --short=7 HEAD)
          tag="v$(date -u +%Y.%m.%d)-${short_sha}"
          if git ls-remote --tags origin "refs/tags/${tag}" | grep -q "refs/tags/${tag}$"; then
            echo "Tag ${tag} already exists on origin — skipping"
            echo "created=false" >> "$GITHUB_OUTPUT"
            echo "tag=${tag}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${tag}" -m "auto: main @ $(git rev-parse HEAD)"
          git push origin "${tag}"
          echo "Pushed tag ${tag}"
          echo "created=true" >> "$GITHUB_OUTPUT"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        if: steps.tag.outputs.created == 'true'
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PAT }}
        run: |
          set -euo pipefail
          gh release create "${{ steps.tag.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --title "${{ steps.tag.outputs.tag }}" \
            --generate-notes
